const sectionValues = ['hot', 'top', 'user'];

const windowsValues = ['day', 'week', 'month', 'year', 'all'];

const sortValues = ['viral', 'top', 'time'];

export { sectionValues, windowsValues, sortValues };
